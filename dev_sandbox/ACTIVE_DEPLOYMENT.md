# 🚀 Active Deployment Configuration

**DO NOT IGNORE THIS FILE.**

This file serves as the source of truth for the active live deployment of this project.

## ✅ Active Vercel Project
- **Project Name:** `saltygold-trials`
- **Vercel Scope:** `matt-ks-projects-09461048`
- **Production URL:** [https://saltygold-trials.vercel.app/](https://saltygold-trials.vercel.app/)
- **Custom Domain:** `results.saltygoldsupply.com` (Target alias)

## ⚠️ Important Rules for AI
1. **ALWAYS** use `npx vercel --prod` to deploy.
2. **NEVER** create a new project. Always link to `saltygold-trials`.
3. If `vercel link` asks to link to an existing project, ALWAYS select **YES** and type **`saltygold-trials`**.
4. Do not use the autogenerated `mass-start-olympic-trials` or other temporary project names.

## Command Shortcut
To deploy updates to the correct live site:
```bash
npx vercel link 
# (Enter 'saltygold-trials' if prompted)
npx vercel --prod
```
